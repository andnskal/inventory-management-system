# 심층 코드 리뷰 — 확정 발견 부록 (2026-06-11)

> 멀티에이전트 리뷰(5차원) + 발견별 적대적 검증. 확정 37건, 기각(거짓양성) 8건.
> 심각도는 **검증 후 교정치**(reviewer 원안에서 과장/축소 보정). 파일경로:라인은 클릭 가능.

## 확정 발견 37건 (심각도순)

| # | 심각도 | 차원 | 위치 | 이슈 | 권고 수정 |
|---|---|---|---|---|---|
| 1 | **HIGH** | data-integrity | `inventory-management/supabase/migrations/001_initial_schema.sql:426-431, 439-444` | 출고 시 재고 음수 차감 무방비 + 보관위치 불일치 시 무반영(조용한 손실) | 출고 시 (1) 해당 stock 행 존재 + 잔량>=quantity를 트리거 내에서 검증해 부족하면 RAISE EXCEPTION으로 트랜잭션을 롤백, (2) 매칭 행이 0건이면 에러를 던지도록 GET DIAGNOSTICS row_count 체크 추가, (3) product_stock에 CHECK(normal_stock>=0) 제약 추가. |
| 2 | **HIGH** | data-integrity | `inventory-management/app/api/inventory/[id]/route.ts:30-33` | 입출고 내역 삭제(DELETE) 시 재고가 원복되지 않아 재고/매출이 영구 왜곡 | AFTER DELETE 트리거를 추가해 삭제된 거래의 type/stock_type/quantity에 따라 재고를 역연산하거나, 물리 삭제 대신 취소 거래(역방향 트랜잭션)를 INSERT하는 방식으로 변경. 단순 delete는 재고 무결성을 깬다. |
| 3 | **HIGH** | data-integrity | `inventory-management/app/api/upload/products/route.ts:46-162` | 엑셀 업로드 부분 실패 시 원자성 없음 — 일부만 반영된 채 성공 응답 | DB 함수(RPC)나 단일 트랜잭션으로 묶어 전부 성공/전부 롤백을 보장하거나, 최소한 options/stock insert·update의 error를 모두 검사해 errors에 누적하고 부분 실패 건수를 응답에 명시. |
| 4 | **HIGH** | data-integrity | `inventory-management/lib/excel.ts:36` | 엑셀 숫자 파싱이 콤마·통화기호·공백을 0으로 둔갑시켜 재고를 0으로 덮어씀 | 문자열이면 replace(/[^0-9.-]/g,'')로 콤마·통화기호·단위 제거 후 파싱하고, 파싱 실패(NaN) 시 0으로 강제하지 말고 해당 행을 errors로 보고하거나 기존 재고를 보존(update 스킵). 음수 결과도 검증. |
| 5 | **HIGH** | error-robustness | `inventory-management/app/api/upload/products/route.ts:65-69, 104-109, 143-160` | 엑셀 업로드: UPDATE/옵션/재고 INSERT의 error를 전부 무시 → 부분 실패가 '성공'으로 집계되는 무음 데이터 손실 | 각 update/insert를 const { error } = await ... 로 받아 검사하고, 실패 시 errors.push(`재고 반영 실패 (${row.product_code}): ${error.message}`) 후 created/updated 카운트를 증가시키지 않는다. 가능하면 행 단위를 RPC 트랜잭션으로 묶어 부분 반영을 막는다. |
| 6 | **HIGH** | error-robustness | `inventory-management/app/api/reports/route.ts:36-41, 78, 131, 148, 171` | 통합 보고서: 5개 섹션 모든 Supabase 조회의 error를 아예 받지 않음 → 조회 실패를 0/빈 값으로 둔갑 | 각 조회를 const { data, error } = await ... 로 받고 error 발생 시 해당 섹션을 에러 표시하거나 500으로 중단한다. 전 섹션 묵음 실패를 막기 위해 최소한 result에 errors 메타를 추가해 클라이언트가 부분 실패를 표시하도록 한다. |
| 7 | **MEDIUM** | api-contract | `inventory-management/app/api/categories/[id]/route.ts:11-27 (PUT) 및 63-79 (DELETE); 동일 블록이 partners/[id], locations/[id], custom-fields/[id], products/[id], finance, audit, settings, reports, upload 전반 반복` | 동일한 Supabase 인증+역할조회 boilerplate가 17개 핸들러에 그대로 복붙 | lib/api/auth.ts에 requireRole(supabase, roles: string[]): Promise<{user, profile} \| NextResponse> 같은 가드 헬퍼를 추출해 각 핸들러 첫 줄에서 `const guard = await requireRole(supabase, ['admin','manager']); if (guard instanceof Response) return guard;` 형태로 1~2줄로 축약한다. 에러 메시지·상태코드도 한 곳에서 통일된다. |
| 8 | **MEDIUM** | data-integrity | `inventory-management/supabase/migrations/001_initial_schema.sql:340-349` | daily_finance_summary 뷰의 매출 정의가 출고 '거래금액(원가 기반일 수 있음)'이라 마진이 실제 이익과 불일치 | 마진을 '출고금액-입고금액'이 아니라 출고수량×(판매단가-매입원가)로 정의하거나, 화면 라벨을 '매출-매입 현금흐름'으로 명확히 바꿔 이익률로 오해되지 않게 한다. 단가 0 출고에 대한 경고도 필요. |
| 9 | **MEDIUM** | data-integrity | `inventory-management/app/api/inventory/route.ts:47-65, 87` | 거래 목록의 상품명/코드 검색이 현재 페이지(20건)에만 적용돼 합계·결과가 틀림 | product 검색을 DB 레벨에서 처리(product_id 사전 조회 후 in 필터, 또는 조인 뷰/RPC)하여 전체 결과에 대해 페이지네이션·합계가 일관되게 하도록 변경. |
| 10 | **MEDIUM** | data-integrity | `inventory-management/app/api/upload/products/route.ts:53-58, 143-159` | 엑셀 중복 product_code 행에 대한 멱등성 결여 — 같은 코드 반복 시 재고가 마지막 행 값으로 덮어쓰기 | 업로드 전 (product_code, option_name) 기준으로 행을 그룹·합산하거나, 동일 키 재등장 시 누적(+=) 정책을 명시. .single() 대신 maybeSingle/limit(1) 사용으로 중복 시 예외 방지. |
| 11 | **MEDIUM** | error-robustness | `inventory-management/app/api/finance/route.ts:44-48, 62-66, 77, 107-110` | 재무 집계: dailySummary/transactions 조회 error를 로깅만 하고 빈 데이터로 200 응답 → 잘못된 0원 집계 노출 | 두 error 중 하나라도 있으면 return Response.json({ error: summaryError?.message ?? txError?.message }, { status: 500 }) 로 실패를 명확히 전달한다. 최소한 응답 본문에 부분 실패 플래그를 포함해 클라이언트가 '0원'과 '조회 실패'를 구분하게 한다. |
| 12 | **MEDIUM** | error-robustness | `inventory-management/app/api/inventory/route.ts:43-65, 87` | inventory GET: product 검색 후필터도 현재 페이지에만 적용 + total을 filtered.length로 보고 → 페이지네이션 깨짐 | 상품명/코드 검색은 join 테이블에 대한 서버측 필터(예: products를 먼저 조회해 product_id 목록을 만들고 .in('product_id', ids))로 옮겨 range 이전에 적용한다. 그러면 count도 정확해진다. |
| 13 | **MEDIUM** | error-robustness | `inventory-management/app/api/products/route.ts:167-169, 186-188` | products POST/PUT: 옵션·커스텀필드 INSERT 실패를 console.error만 하고 product는 성공(201/200) 반환 → 부분 저장 | 옵션/커스텀필드 INSERT error 발생 시 부분 실패를 응답에 포함(예: { product, warnings: [...] })하거나, 전체를 RPC 트랜잭션으로 묶어 실패 시 product 생성까지 롤백한다. |
| 14 | **MEDIUM** | nextjs16-react19 | `inventory-management/app/(dashboard)/page.tsx:144-156` | 대시보드 페이지의 N+1 쿼리 — 재고 보유 상품마다 마지막 출고일을 개별 await 조회 | 상품별 마지막 출고일을 한 번의 집계 쿼리(예: product_id별 MAX(transaction_date) 뷰/RPC)로 가져오거나, 최소한 개별 쿼리를 Promise.all로 병렬화하여 순차 await 라운드트립을 제거하라. |
| 15 | **MEDIUM** | security-auth | `inventory-management/app/api/upload/products/route.ts:26-162` | 엑셀 업로드 라우트: rows 개수/내용 검증 부재로 대량요청 DoS 및 순차 N+1 증폭 | rows.length 상한(예: 1000~5000)과 요청 본문 크기 제한을 두고, 각 필드의 타입/범위(특히 normal_stock·pending_shortage_stock 숫자 여부, 음수 차단)를 zod 등으로 검증한다. 가능하면 행 단위 순차 쿼리를 배치 upsert로 묶어 왕복 수를 줄인다. |
| 16 | **MEDIUM** | security-auth | `inventory-management/app/api/inventory/route.ts:156-171` | inventory_transactions INSERT RLS가 WITH CHECK(true) — created_by 위조는 API 주입으로만 보완(우회 시 위조 가능) | inventory_transactions INSERT 정책을 WITH CHECK (created_by = auth.uid())로 강화하여 위조를 DB 레벨에서 차단한다. 클라이언트 직접 INSERT 경로가 필요 없다면 해당 권한을 회수한다. |
| 17 | **LOW** | api-contract | `inventory-management/app/api/reports/route.ts:197; cf. products/route.ts:83-91, categories/route.ts:25, upload/products/route.ts:164-170, reports/pdf/route.ts:18-21` | 성공 응답 envelope가 라우트마다 제각각 — 일관된 표준 형태 부재 | 공통 응답 스키마를 정한다. 예: 성공은 항상 { data: T, meta?: {...} }, 목록은 { data: T[], meta: { total, page, pageSize } }. lib/api/response.ts에 ok(data, init?)·created(data)·fail(message, status) 헬퍼를 만들어 전 라우트가 동일 envelope를 반환하도록 통일한다. |
| 18 | **LOW** | api-contract | `inventory-management/app/api/products/route.ts:4-13 (auth 없음); cf. partners/route.ts:4-16 (auth 없음) vs categories/route.ts:4-13, locations/route.ts:4-13, custom-fields/route.ts:4-13, settings/route.ts:4-13 (모두 auth 요구)` | GET 핸들러 인증 정책 비일관 — products·partners 목록 GET만 auth 체크 누락(나머지 GET은 인증 요구) | GET 인증 정책을 명시적으로 한 가지로 통일한다(전부 미들웨어 인증에 위임하고 라우트 내 중복 getUser를 제거하거나, 반대로 전 GET에 requireAuth 가드를 일괄 적용). 어느 쪽이든 18개 라우트가 동일 규칙을 따르게 한다. |
| 19 | **LOW** | api-contract | `inventory-management/app/api/upload/products/route.ts:164-170; cf. settings/route.ts:78-91 및 201 사용처 products/route.ts:192, categories/route.ts:72, partners/route.ts:107, locations/route.ts:71, custom-fields/route.ts:75, inventory/route.ts:179` | 생성 엔드포인트 상태코드 불일치 — CRUD POST는 201, upload POST(생성 포함)와 settings PUT(insert 포함)은 200 | 벌크/업서트처럼 생성·수정이 혼재된 엔드포인트는 200 + 결과 카운트로 명시 통일하거나, 단건 생성은 201로 통일한다는 규칙을 문서화하고 created/[id]/route 헬퍼(created())로 강제한다. |
| 20 | **LOW** | api-contract | `inventory-management/app/api/categories/route.ts:54-64 (무검증); cf. partners/route.ts:75-87 및 inventory/route.ts:140-152 (수동검증)` | 입력 검증 방식이 무검증·부분 수동검증으로 혼재 (zod 등 스키마 검증 부재) | 라우트별 zod 스키마(또는 공통 validate(schema, body) 헬퍼)를 도입해 모든 POST/PUT이 동일하게 파싱→실패 시 400 + 필드별 메시지를 반환하도록 표준화한다. |
| 21 | **LOW** | data-integrity | `inventory-management/app/api/finance/route.ts:99-105` | 기간 필터된 상품별 마진율 0 나눗셈/음수 처리로 순위·마진율이 오도됨 | 매출 0인데 매입>0인 경우를 별도 표기(예: 'N/A' 또는 -100% 손실)하고, 정렬 시 이런 행을 마진율 최하위로 보내 손실 상품이 묻히지 않게 한다. |
| 22 | **LOW** | data-integrity | `inventory-management/app/api/inventory/route.ts:154, 167` | 입출고 단가 누락 시 total_price가 0으로 저장되어 매출/매입 집계 누락 | 출고(또는 입고)에서 금액 집계가 중요한 경우 unit_price>0을 필수화하거나, 단가 0 거래를 집계/리포트에서 명시적으로 분리 표시. 최소한 단가 0 등록 시 경고. |
| 23 | **LOW** | data-integrity | `inventory-management/app/api/audit/route.ts:39-42` | audit 조회의 날짜 경계가 타임존 미지정 문자열로 비교돼 경계 거래 누락/중복 가능 | 명시적 타임존(`${dateFrom}T00:00:00+09:00`)을 붙이거나, lte 대신 다음날 00:00:00 미만(lt)으로 반열린 구간을 사용해 밀리초 경계 누락을 방지. |
| 24 | **LOW** | error-robustness | `inventory-management/lib/excel.ts:24-28` | excel.ts: XLSX.read/sheet_to_json에 try/catch·빈 워크북 가드 없음 → 손상 파일이 throw로 전파 | if (!workbook.SheetNames.length) throw new Error('시트가 없습니다') / if (!sheet) throw new Error('빈 시트입니다') 처럼 명시적 가드를 추가하고, 헤더 매핑 결과가 0건일 때(컬럼 불일치)도 구분된 에러를 던져 호출부에서 안내 메시지를 다르게 표시한다. |
| 25 | **LOW** | error-robustness | `inventory-management/components/inventory/transaction-form-dialog.tsx:294-300, 309-315` | 수량/단가 입력 NaN 처리 일관성 결여: parseInt(빈/소수 입력)이 \|\| 0으로 묵살되어 의도치 않은 0 저장 | Number(value)로 받아 Number.isFinite() 검사 후 음수/소수 정책을 명시적으로 적용한다(예: 수량은 정수·양수 강제, 단가는 음수 거부). 빈 문자열은 0이 아니라 미입력 상태로 구분 가능하게 처리한다. |
| 26 | **LOW** | error-robustness | `inventory-management/app/api/inventory/route.ts:147-154` | inventory POST: total_price 계산 시 quantity/unit_price의 숫자 타입을 검증하지 않음 | const q = Number(quantity), up = Number(unit_price)로 변환 후 Number.isFinite(q) && q > 0, Number.isFinite(up) && up >= 0를 검증하고 실패 시 400을 반환한다. total_price는 검증된 숫자로 계산한다. |
| 27 | **LOW** | error-robustness | `inventory-management/app/api/products/route.ts:34-36, 64-85` | products GET: low/normal 상태 필터를 현재 페이지(range) 내에서만 수행 → 페이지네이션과 결합 시 누락·count 불일치 | 재고 상태 필터는 DB 뷰(product_stock_summary)나 RPC로 옮겨 서버에서 필터·count를 일치시킨다. 불가하면 status 필터 사용 시 total을 filteredProducts 기준으로 보정한다. |
| 28 | **LOW** | error-robustness | `inventory-management/app/api/settings/route.ts:58-62` | settings PUT: upsert 분기의 .single() 결과 error를 검사하지 않아 조회 실패를 '없음'으로 오판 | upsert를 단일 호출(supabase.from('settings').upsert({ key, value, updated_by }, { onConflict: 'key' }))로 대체하거나, maybeSingle()을 쓰고 error를 검사해 조회 실패와 '미존재'를 구분한다. |
| 29 | **LOW** | nextjs16-react19 | `inventory-management/app/api/products/route.ts:4-13` | GET 라우트 핸들러 4개에 인증/인가 체크 누락 — RLS에만 의존(정확성보다 인가 이슈지만 라우트 핸들러 일관성 결함) | 각 GET 핸들러 도입부에 다른 핸들러와 동일하게 `const { data: { user } } = await supabase.auth.getUser(); if (!user) return Response.json({ error: '인증이 필요합니다.' }, { status: 401 })`를 추가하여 라우트 레벨 인증을 일관화하라. RLS는 방어선으로 유지하되 유일한 방어선으로 두지 말 것. |
| 30 | **LOW** | security-auth | `inventory-management/app/api/products/route.ts:4-96` | products GET 핸들러에 인증/인가 체크 부재 — RLS(anon 키)에만 의존 | GET 시작부에 const { data: { user } } = await supabase.auth.getUser(); if (!user) return 401; 을 추가하여 POST/PUT/DELETE와 동일하게 라우트 레벨 인증을 강제한다. 읽기 권한을 RLS에만 위임하지 말 것. |
| 31 | **LOW** | security-auth | `inventory-management/app/api/inventory/route.ts:4-101` | inventory GET 핸들러 인증 체크 부재 — 전체 입출고 거래 내역을 RLS에만 의존해 노출 | GET 진입 시 getUser() 인증 검사를 추가한다. 거래 데이터는 민감하므로 finance/audit 라우트처럼 role 인가까지 고려한다. |
| 32 | **LOW** | security-auth | `inventory-management/app/api/products/route.ts:41, 62, 151` | DB 에러 메시지를 클라이언트 응답에 그대로 노출(error.message) | 클라이언트에는 일반화된 메시지만 반환하고(catch 블록과 동일), 상세 error는 console.error로 서버 로그에만 남긴다. 공용 에러 핸들러 유틸로 통일한다. |
| 33 | **LOW** | security-auth | `inventory-management/app/api/products/route.ts:121-148` | products POST/PUT: product_code·name·가격 등 핵심 필드 입력 검증 부재로 미검증 데이터 DB 삽입 | insert/update 전에 product_code·name 필수 검증, 가격/재고 음수 차단, 문자열 길이 제한을 zod 등 스키마로 강제한다. 다른 라우트의 검증 패턴과 일관화한다. |
| 34 | **LOW** | security-auth | `inventory-management/app/api/settings/route.ts:4-30` | settings/custom-fields/categories/locations GET: 인증만 있고 역할 인가 없음(읽기 권한 과다) | settings 등 민감 테이블 GET에도 PUT과 동일한 role 인가를 적용하거나, 민감 키를 응답에서 제외한다. 노출이 의도된 공개 설정만 별도로 분리한다. |
| 35 | **LOW** | security-auth | `inventory-management/app/api/audit/route.ts:38-43` | audit GET: 날짜 파라미터를 검증 없이 changed_at 필터 문자열로 보간 | 날짜 파라미터를 ISO yyyy-mm-dd 정규식 또는 Date 파싱으로 검증한 뒤 사용하고, 검증 실패 시 400을 반환한다. 파싱 에러의 raw 메시지는 노출하지 않는다. |
| 36 | **LOW** | security-auth | `inventory-management/app/api/inventory/route.ts:13-14` | pageSize 등 페이지네이션 파라미터 상한 부재 — 대량 응답 유발 가능 | pageSize를 Math.min(Math.max(parsed, 1), 100) 형태로 clamp하고 page도 1 이상으로 보정한다. NaN 입력에 대한 기본값 처리도 추가한다. |
| 37 | **LOW** | security-auth | `inventory-management/app/api/products/route.ts:29-31` | products GET: category_id 등 외부 입력을 .eq에 직접 전달(타입 검증 부재) | UUID 형식의 id 파라미터는 정규식/zod로 검증하거나 캐스팅 에러를 잡아 일반화된 400으로 응답한다. raw DB 에러 노출을 함께 제거한다. |

## 기각된 발견 (적대적 검증으로 무효/거짓양성 판정) — 8건

> 검증 엄밀성의 증거. 아래는 "문제처럼 보였으나 미들웨어/RLS/프레임워크가 이미 방어"로 판명된 항목.

| # | 원안 심각도 | 차원 | 위치 | 기각 사유(요약) |
|---|---|---|---|---|
| 1 | high | security-auth | `inventory-management/app/api/partners/route.ts:4-22` | partners GET 핸들러 인증 부재 + 검색어를 .or() 필터에 직접 보간(PostgREST 필터 인젝션) — false positive — 인증은 미들웨어가 전역 강제하고, .or() 인젝션은 RLS USING(true) 환경에서 권한 경계를 넘지 못함. 실질 보안 영향 없음, 입력 위생 수준의 low. |
| 2 | low | nextjs16-react19 | `inventory-management/app/api/products/[id]/route.ts:4-8` | 동적 세그먼트 params·searchParams·cookies()는 Next 16 비동기 규약을 모두 올바르게 준수 (위반 0건) — 이 finding은 "버그 발견"이 아니라 "위반 0건 확인" 보고서이며, 실제 코드와 대조한 결과 보고서의 사실관계는 전부 정확하다. 따라서 수정이 필요한 진짜 문제(isReal)는 존재하지 않는다. 동시에, 이 항목 자체는 어떤 결함도 지목하지 않으므로 actionable한 finding으로서는 무효(invalid)다. [교차 검증 결과 — 모두 일치] 1) 동적 params Promise화: app/api 트리 전체를 grep한 결과 6개 [id] 라우트의 |
| 3 | low | nextjs16-react19 | `inventory-management/lib/supabase/server.ts:1-27` | 'use client' 경계 정상 — 서버 전용 supabase 클라이언트/env가 클라이언트 컴포넌트로 누수되지 않음 — 이 발견은 "문제 없음(no-issue)"을 보고하는 부정적(negative) 결과이며, 실제 파일 확인 결과 그 판단이 정확하다. 따라서 수정이 필요한 결함(isReal)은 아니며 severity는 invalid(=실재 문제 아님)로 교정한다. 근거(코드 인용): 1) server.ts(1-27)는 서버 전용 API만 사용한다. L2 `import { cookies } from 'next/headers'`는 서버 전용 모듈이고, 클라이언트 컴포넌트가 이를 im |
| 4 | low | nextjs16-react19 | `inventory-management/next.config.ts:3-7` | 캐싱 관련 export(dynamic/revalidate) 누락은 이 코드베이스에서 정확성 결함 아님 — cookies() 사용으로 전 페이지/라우트가 자동 동적 — invalid_non_finding |
| 5 | medium | data-integrity | `inventory-management/supabase/migrations/001_initial_schema.sql:368-394` | 감사 로그가 product_stock·inventory_transactions 변경의 행위자를 SECURITY DEFINER 트리거의 auth.uid()로 기록 — 누락/오기 위험 — False positive. 발견의 핵심 기술 전제가 틀렸고, 나머지는 정상 동작을 결함으로 오인하거나 존재하지 않는 미래 코드를 추측한 것이다. 1) SECURITY DEFINER ≠ auth.uid() 위협 (핵심 오독). 트리거 함수(001_initial_schema.sql:368-386)는 `INSERT INTO audit_logs (...) VALUES (..., auth.uid())`를 쓴다. SECURITY DEFINER는 함수의 실행 *권한(rol |
| 6 | medium | error-robustness | `inventory-management/components/finance/finance-page-client.tsx:55-56` | 클라이언트가 응답 본문의 형태를 검증 없이 신뢰: data.dailySummary/data.logs 등 undefined 가능 값을 그대로 setState — 현존 버그가 아님(방어적 코딩 부재일 뿐). RLS/미들웨어와 무관하며, 응답 형태 계약(contract)의 문제로 정확히 판단해야 한다. 근거(코드 인용): 1) finance 라우트(app/api/finance/route.ts)는 성공 시 항상 배열을 보장한다:    line 107-110 `return Response.json({ dailySummary: dailySummary ?? [], productAggregates })`    - `dailySumm |
| 7 | low | error-robustness | `inventory-management/components/audit/audit-page-client.tsx:60-67` | audit/finance 등 클라이언트: 낙관적 업데이트 후 fetch 실패 시 롤백 부재(직접적 위험은 낮음) — 거짓 양성(false positive). 현재 코드 기준 실제 버그가 아니며, 발견이 가정한 두 전제가 모두 코드와 불일치한다. 1) "data.logs undefined → logs.map 크래시" 경로는 도달 불가능하다. 호출 대상인 `app/api/audit/route.ts`는 성공 응답에서 항상 `logs: logs ?? []`(55-56행)를 반환해 undefined를 원천 차단한다. 오류 경로(401/403/500)는 모두 `{ error: ... } |
| 8 | low | api-contract | `inventory-management/app/api/inventory/[id]/route.ts:4 (DELETE만 존재, GET/PUT 없음); cf. products/[id] (PUT+DELETE), settings/route.ts (collection에 GET+PUT, [id] 없음)` | HTTP 메서드 핸들러 분산·누락으로 동일 리소스의 메서드 계약이 비대칭 — 코드 구조 서술 자체는 사실이나, "문제(defect)"는 아니다 — false positive. [사실 확인] - inventory/[id]/route.ts: 파일 전체(4~47줄)에 `export async function DELETE` 단 하나만 존재. GET/PUT 미정의 확인. - products/[id]/route.ts: `export async function PUT`(4줄) + `export async function DELETE`(124줄) 둘  |
